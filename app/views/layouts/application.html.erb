<!DOCTYPE html>
<html>
  <head>
    <title>Museo</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
    <%= yield %>
    <% if current_user %>
      User: <%= current_user.username %>
      <%= link_to "Logout", '/logout', method: :get %>
    <% end%>
    <%= link_to "Inicio", '/', method: :get %>
    
  </body>
</html>
